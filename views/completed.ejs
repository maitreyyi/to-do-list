<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Completed List</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
</head>
<body>
    <!--header-->
    <div class="container">
        <header class="d-flex justify-content-center py-3">
          <ul class="nav nav-pills">
            <li class="nav-item"><a href="/" class="nav-link" aria-current="page">To-do</a></li>
            <li class="nav-item"><a href="../completed" class="nav-link active">Completed</a></li>
          </ul>
        </header>
    </div>

    <div class = "container">
        <ul class="list-group">
            <% if(locals.activities){ %>
                <% activities.forEach(activity => { %>
                    <li class="list-group-item">
                        <input class="form-check-input me-1" type="checkbox" name = "checkbox" value="<%=activity._id %>" id="firstCheckbox">
                        <label class="form-check-label" for="firstCheckbox"> <%=activity.name %></label>
                    </li>
                <% }); %>
            <%} %>
        </ul>
        <!--footer-->
        <%- include("partials/footer.ejs")%>
    </div>
    
</body>

</html>